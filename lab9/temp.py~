import math
import numpy as np
from random import choice

import pygame

WIDTH = 800
HEIGHT = 600
screen = pygame.display.set_mode((WIDTH, HEIGHT))
screen.fill(0xFFFFFF)

pygame.draw.line(
    screen,
    (255, 0, 0),
    (20, 450),
    (50, 500),
    10)
